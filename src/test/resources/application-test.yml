spring:
  datasource:
    url: jdbc:mysql://localhost:3306/unicorp_test?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  flyway:
    enabled: false

# 禁用安全配置，方便测试
security:
  basic:
    enabled: false

# 测试环境日志级别
logging:
  level:
    root: INFO
    com.csu.unicorp: DEBUG
    org.hibernate.SQL: DEBUG
    org.springframework.test: DEBUG
    org.flywaydb: DEBUG 